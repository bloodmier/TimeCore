# ===============================
# Fortnox API configuration
# ===============================

CLIENT_ID=your_fortnox_client_id
CLIENT_SECRET=your_fortnox_client_secret

REDIRECT_URI=http://localhost:5000/api/fortnox/oauth/callback

# Space-separated Fortnox scopes
SCOPES=archive connectfile article customer invoice companyinformation inbox

# Environment flag
IN_PRODUCTION=false
